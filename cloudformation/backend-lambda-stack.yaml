AWSTemplateFormatVersion: '2010-09-09'
Description: GatherQuizバックエンド(Lambda + API Gateway)用CloudFormationテンプレート

Parameters:
  VpcStackName:
    Type: String
    Description: 出力をインポートするVPC CloudFormationスタックの名前
  RdsStackName:
    Type: String
    Description: 出力をインポートするRDS CloudFormationスタックの名前
  DbUser:
    Type: String
    Description: RDSデータベースのマスターユーザー名
    NoEcho: true
  DbPassword:
    Type: String
    Description: RDSデータベースのマスターパスワード
    NoEcho: true
  EcrImageUri:
    Type: String
    Description: Lambda関数のECRイメージURI

Resources:
  # IAM Role for Lambda Execution
  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${AWS::StackName}-LambdaExecutionRole
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole

  # Lambda Function from Container Image
  BackendLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${AWS::StackName}-BackendLambda
      PackageType: Image
      Code:
        ImageUri: !Ref EcrImageUri
      Role: !GetAtt LambdaExecutionRole.Arn
      VpcConfig:
        SecurityGroupIds:
          - !ImportValue
              'Fn::Sub': '${VpcStackName}-LambdaSecurityGroupId'
        SubnetIds:
          'Fn::Split':
            - ','
            - !ImportValue
                'Fn::Sub': '${VpcStackName}-PrivateSubnetIds'
      Environment:
        Variables:
          DATABASE_URL: !Sub
            - "postgresql://${DbUser}:${DbPassword}@${DbEndpoint}:${DbPort}/gatherquiz?schema=public"
            - DbEndpoint: !ImportValue
                'Fn::Sub': '${RdsStackName}-DbEndpoint'
              DbPort: !ImportValue
                'Fn::Sub': '${RdsStackName}-DbPort'
      Timeout: 60
      MemorySize: 1024

  # HTTP API Gateway
  HttpApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: !Sub ${AWS::StackName}-HttpApi
      ProtocolType: HTTP
      CorsConfiguration:
        AllowOrigins:
          - "*" # 本番環境では、フロントエンドのドメインのみに制限することを推奨
        AllowMethods:
          - GET
          - POST
          - PUT
          - PATCH
          - DELETE
          - OPTIONS
        AllowHeaders:
          - Content-Type
          - Authorization

  # API Gateway Integration with Lambda
  HttpApiIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt BackendLambda.Arn
      PayloadFormatVersion: '2.0'

  # API Gateway Default Route
  HttpApiDefaultRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: $default
      Target: !Join
        - /
        - - integrations
          - !Ref HttpApiIntegration

  # API Gateway Stage
  HttpApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref HttpApi
      StageName: $default
      AutoDeploy: true

  # Permission for API Gateway to invoke Lambda
  LambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt BackendLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*"

Outputs:
  ApiGatewayEndpoint:
    Description: API GatewayエンドポイントのURL
    Value: !GetAtt HttpApi.ApiEndpoint
    Export:
      Name: !Sub ${AWS::StackName}-ApiGatewayEndpoint