# デプロイ検証記録

## 2026-09-09: dev認証と本番環境

devのCloudFront配信関数だけを更新し、DB・API Lambdaのコードは維持した。
Basic認証はHTMLと静的アセットに適用。API・投稿画像は既存のアクセス仕様を維持する。
認証情報はSSM `/gather-quiz-dev/frontend-password` に保管し、ユーザー名は`dev`。

- 未認証・誤認証は401、正しい認証はHTML・JSを200で取得できることを確認
- CloudFront標準ドメインからも認証を迂回できないことを確認
- devでブラウザの画面遷移・SPAリロードを確認
- インフラ7件、バックエンド70件、フロントエンド72件の単体テストが成功
- 双方のlint・ビルド、運用スクリプトの型チェック、CloudFormation構文検証が成功
- SQLite単体テストは既存ネイティブ依存関係に合わせNode.js 22で実行。本番ZIPはNode.js 24で生成

本番 `https://gather-quiz.june-yamamoto.com` は `gather-quiz-prod` とそのartifacts / certificate / ciの4スタックで作成完了。
devとはDB・S3・Lambda・CloudFrontを分離し、新規DBを現行スキーマで初期化した。GitHub OIDCプロバイダーのみ共有。

- HTTPS公開配信、SPA直リンク・リロード、JSアセット取得、APIのJSON 404とDB接続が成功
- 大会作成・主催者ログイン・参加者登録とログイン・クイズ作成・ボード取得が成功
- 署名付き画像アップロード・CloudFront画像取得、バックアップ・隔離スキーマ復元検証が成功
- 検証用画像・業務データを削除後、バックアップでTournament / Participant / Quizがすべて0件と確認
- アプリスタック作成544.12秒、DB初期化0.73秒、CI作成30.59秒、フロント4ファイル転送0.29秒
- GitHub変数`AWS_DEPLOY_PROD_ROLE_ARN`に本番CIロールを登録済み。既存dev変数は維持

環境選択を追加したワークフローを実装した。本構築はローカルから実行し、新ワークフロー自体の実行は未実施。
今回ローカルE2E一式は再実行せず、変更対象の単体検証と両デプロイ環境のブラウザ確認を実施した。

## 2026-09-08: dev初期構築

実施日: 2026-09-08（日本時間）。対象: `gather-quiz-dev`、AWS東京リージョン。

## 調査と反映

開始時、旧GatherQuizのLambda・RDS・CloudFront・ECRは存在しなかった。既存のRoute53ホストゾーンを利用し、無関係のリソースは変更していない。
旧ドキュメントに記載されたAPI IDや稼働状態は再利用せず、CloudFormationのOutputsから対象を取得した。
ZIP方式の最終構成で4スタックを新規構築し、その後の差分デプロイも確認した。

- 公開URL: https://dev.gather-quiz.june-yamamoto.com
- RDS: PostgreSQL 17.9、db.t4g.micro、Single-AZ、暗号化gp3 20GiB、非公開
- Lambda: Node.js 24、ARM64、ZIP（API 256MB／運用512MB）
- DBデータは検証用のものを削除し、検証後の3テーブルの件数はいずれも0件
- GitHub OIDCロールとリポジトリ変数`AWS_DEPLOY_ROLE_ARN`設定済み
- AWS向けワークフローは手動起動のみ。PRがmainへマージされた後にActionsから選択可能

## 実環境の動作確認

- HTTPSトップ画面、SPA直リンクとリロード、静的アセットのキャッシュ制御
- 存在しないAPIがHTMLに変換されずJSONエラーを返すこと
- 大会作成、主催者ログイン、参加者登録・ログイン、クイズ作成、問題ボード取得
- 署名付きURLによるPNGアップロードとCloudFront経由の画像取得
- 3テーブルにデータが入った状態のバックアップと、隔離スキーマへの復元・チェックサム一致・外部キー整合性
- テストデータ削除後、0件の実DBに対するバックアップからの復元と復元直前バックアップ作成
- Playwrightで公開サイトから大会作成画面へ遷移し、リロード後も表示でき、ブラウザ例外がないこと

検証スクリプトは`scripts/smoke.ts`、`scripts/browser-smoke.ts`。通常のActionsではデータを書かない`--read-only`を実行する。
今回の実環境構築・更新は作業環境から同じデプロイスクリプトを実行した。新ワークフロー自体の手動実行はmainへのマージ後に行う。

## 時間の実測

単一環境での参考値。依存インストール・テスト・Actionsキュー時間を含まない。

| 操作 | 実測 |
| --- | ---: |
| アプリスタック初回作成（RDS・CloudFrontを含む） | 544.36秒 |
| ACM初回発行 | 163.19秒 |
| 最終バックエンドZIPビルド | 3.15秒 |
| コード変更ありのLambda更新 | 11.39秒 |
| 変更なしのLambda確認・更新省略 | 0.23秒 |
| 最終フロントエンドビルド | 10.48秒 |
| 最終フロントエンド差分転送（4ファイル中2件） | 0.24秒 |
| 変更なしのフロントエンド差分確認（4ファイル、転送0件） | 0.11秒 |
| 最終CloudFrontルート変更を含むスタック更新 | 61.04秒 |

ZIPは1,775,706バイト。旧Docker方式は環境が削除済みで同条件の実測がないため、短縮率は算出していない。
Dockerビルド・ECR push・全ファイル再転送・Invalidation待ちを通常の更新経路から除いた。

## 制約

ローカル検証はバックエンド41件、フロントエンド34件、インフラ4件、Playwright E2E 7件が成功。双方のlint・ビルドと運用スクリプトの型チェックも成功した。
双方のロックファイルに欠けていたOS別パッケージを同一バージョンで補完し、Windowsでのクリーンインストール・単体テスト・E2Eを確認した。クリーンインストール後の成果物を実環境へ反映済み。

バックアップは小規模データ用のgzip JSONで、画像・DDL・DBロールを含まない。上限や復元手順は[インフラ運用](インフラ運用.md)を参照。
負荷試験・Multi-AZ障害試験・Dev Container再構築は今回の動作確認に含まない。
公開サイトでの検証は専用の合成データを使用した。実利用者のデータを復元して上書きする試験は行っていない。
